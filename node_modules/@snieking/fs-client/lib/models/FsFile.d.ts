import { ChunkIndex } from './Chunk';
export declare class FsFile {
    static fromLocalFile(path: string): Promise<FsFile>;
    static fromData(data: Buffer): FsFile;
    static fromChunks(chunks: ChunkIndex[]): FsFile;
    private static BYTES;
    private static sliceIntoChunks;
    readonly hash: Buffer;
    readonly chunks: Buffer[];
    readonly size: number;
    readonly data: Buffer;
    private constructor();
    getChunk(index: number): Buffer;
    numberOfChunks(): number;
}
