import { KeyPair } from 'ft3-lib';
import { Filehub } from './Filehub';
import { IChunkLocation } from '../models/FilechainLocation';
import Filechain from './Filechain';
export declare class FilehubAdministrator {
    private readonly filehub;
    private readonly admin;
    constructor(filehub: Filehub, admin: KeyPair);
    /**
     * Registers a Filechain to persist files in.
     *
     * @param user that is an admin of the filehub.
     * @param rid of the filechain.
     */
    registerFilechain(rid: string, url: string): Promise<void>;
    /**
     * Disables a Filechain
     */
    disableFilechain(rid: string): Promise<void>;
    /**
     * Enables a Filechain
     */
    enableFilechain(rid: string, url: string): Promise<void>;
    /**
     * Gets a Filechain from a filechain location.
     */
    getFilechain(filechainLocation: IChunkLocation): Filechain;
}
