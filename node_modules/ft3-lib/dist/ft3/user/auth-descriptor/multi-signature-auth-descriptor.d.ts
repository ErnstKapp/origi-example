import { AuthDescriptor, Flags, FlagsType, PubKey } from "../account";
import AuthDescriptorRule from "./auth-descriptor-rule";
export default class MultiSignatureAuthDescriptor implements AuthDescriptor {
    readonly rule: AuthDescriptorRule | null;
    pubkeys: PubKey[];
    flags: Flags;
    signaturesRequired: number;
    constructor(pubkeys: PubKey[], signaturesRequired: number, flags: FlagsType[], rule?: AuthDescriptorRule | null);
    readonly signers: PubKey[];
    readonly id: Buffer;
    toGTV(): any[];
    hash(): Buffer;
}
