import { AuthDescriptor, Flags, FlagsType, PubKey } from "../account";
import AuthDescriptorRule from "./auth-descriptor-rule";
export default class SingleSignatureAuthDescriptor implements AuthDescriptor {
    readonly rule: AuthDescriptorRule | null;
    pubkey: PubKey;
    flags: Flags;
    constructor(pubkey: PubKey, flags: FlagsType[], rule?: AuthDescriptorRule | null);
    readonly signers: PubKey[];
    readonly id: Buffer;
    toGTV(): any[];
    hash(): Buffer;
}
