import Asset from "./asset";
import Blockchain from "../core/blockchain/blockchain";
export default class AssetBalance {
    amount: number;
    asset: Asset;
    constructor(amount: number, asset: Asset);
    static getByAccountId(id: Buffer, blockchain: Blockchain): Promise<AssetBalance[]>;
    static getByAccountAndAssetId(accountId: any, assetId: any, blockchain: Blockchain): Promise<AssetBalance>;
    static giveBalance(accountId: any, assetId: any, amount: any, blockchain: Blockchain): Promise<void>;
}
