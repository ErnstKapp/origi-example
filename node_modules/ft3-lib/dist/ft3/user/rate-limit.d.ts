import Blockchain from "../core/blockchain/blockchain";
export default class RateLimit {
    points: number;
    last_upgrade: number;
    constructor(points: number, last_upgrade: number);
    getRequestsLeft(): number;
    static execFreeOperation(accountId: Buffer, blockchain: Blockchain): Promise<void>;
    static getByAccountRateLimit(accountId: Buffer, blockchain: Blockchain): Promise<RateLimit>;
    static givePoints(accountId: Buffer, points: number, blockchain: Blockchain): Promise<void>;
    static getLastTimestamp(blockchain: Blockchain): Promise<number>;
    static getPointsAvailable(points: number, lastOperation: number, blockchain: Blockchain): Promise<number>;
}
