export default class PaymentHistoryEntry {
    readonly isInput: boolean;
    readonly delta: number;
    readonly asset: string;
    readonly assetId: Buffer;
    readonly chainId: Buffer;
    readonly other: any[];
    readonly timestamp: Date;
    readonly transactionId: Buffer;
    readonly blockHeight: number;
    constructor(isInput: boolean, delta: number, asset: string, assetId: Buffer, other: any[], timestamp: Date, transactionId: Buffer, blockHeight: number);
    adaptForSerialization(): {};
}
