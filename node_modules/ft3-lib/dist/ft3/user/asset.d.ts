import Blockchain from "../core/blockchain/blockchain";
export default class Asset {
    name: string;
    chainId: Buffer;
    constructor(name: string, chainId: Buffer);
    readonly id: any;
    static register(name: string, chainId: Buffer, blockchain: Blockchain): Promise<Asset>;
    static getByName(name: string, blockchain: Blockchain): Promise<Asset[]>;
    static getById(id: Buffer, blockchain: Blockchain): Promise<Asset>;
    static getAssets(blockchain: Blockchain): Promise<any>;
}
