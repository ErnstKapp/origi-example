import ConnectionClient from "../connection-client";
import RateLimitInfo from "./rate-limit-info";
export default class BlockchainInfo {
    name: string;
    website: string;
    description: string;
    rateLimitInfo: RateLimitInfo;
    constructor(name: string, website: string, description: string, rateLimitInfo: RateLimitInfo);
    static getInfo(connection: ConnectionClient): Promise<BlockchainInfo>;
}
